// Immediately Invoked Function Expression IIFE

(function chai(){
    //This is Named IIFE
    console.log("Learned about IIFE in JS");
    
})();// semicolon is must if there are two IIFE function need to write
// Semicolon separates each IIFE and executes them one by one

// While writing IIFE there are two (), () parenthesis
// one () wrapping up the function definition
//Other () is to execution of wrapped function

(() => {
    console.log("Getting it clearly");
    
})()

